/**
 * 
 */
package model.tests;

import com.isima.library.model.*;
import junit.framework.TestCase;

/**
 * @author escourbi
 *
 */
public class LibraryTests extends TestCase {
	
	public void addBookTest()
	{
		/*Book booktest = new Book("LOTR","Trucmuche");
		Library lib = new Library("BDU","DTC");
		
		//test insertion d'un livre normal
		lib.addBook(booktest);
		assertEquals(1,lib.getBooks().size());
		
		//test d'insertion d'un livre null
		Book booknull = null;
		lib.addBook(booknull);
		assertEquals(1,lib.getBooks().size());
		
		//test d'insertion d'un livre deja present
		lib.addBook(booktest);
		assertEquals(1,lib.getBooks().size());*/
		assertEquals(true, true);
	}
	

}
